<div id="navbar">
	<nav class="navbar navbar-expand-lg">
		<li class="navbar">
			<a class="navbar btn btn-primary" href="{{ path('home') }}">Accueil</a>
		</li>
		<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse" id="navbarColor01">
			<ul class="navbar-nav mr-auto">
				<li class="navbar">
					<a class="navbar btn btn-primary" href="{{ path('presentation') }}">Qui sommes nous?</a>
				</li>
				<li class="navbar">
					<div class="btn-group " role="group" aria-label="Button group with nested dropdown">
						<span class="btn btn-primary">Produits</span>
						<div class="btn-group" role="group">
							<button id="btnGroupDrop1" type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"></button>
							<div class="dropdown-menu" aria-labelledby="btnGroupDrop1">
								{% for gamme in gammes %}
									{% if gamme.statut == 1 %}

										<a class="dropdown-item" href="{{ path('gamme_index', {'id':gamme.id}) }}">{{ gamme.nom }}</a>
									{% endif %}

								{% endfor %}
							</div>
						</div>
					</div>
				</li>
				<li class="navbar">
					<div class="btn-group" role="group" aria-label="Button group with nested dropdown">
						<span class="btn btn-primary">Marques</span>
						<div class="btn-group" role="group">
							<button id="btnGroupDrop1" type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"></button>
							<div class="dropdown-menu" aria-labelledby="btnGroupDrop1">
								{% for marque in marques %}
									{% if marque.statut == 1 %}

										<a class="dropdown-item" href="{{ path('marque_index', {'id':marque.id}) }}">{{ marque.nom }}</a>
									{% endif %}
								{% endfor %}

							</div>
						</div>
					</div>
				</li>
				<li class="navbar">
					<a class="navbar btn btn-primary" href="{{ path('gallerie') }}">Réalisations</a>
				</li>
				{% if app.user == null %}
					<li class="navbar">
						<a class="navbar btn btn-primary" href="{{ path('contact') }}">Contact</a>
					</li>
				{% endif %}
				{% if is_granted('ROLE_ADMIN') %}
					<li class="navbar">
						<div class="btn-group " role="group" aria-label="Button group with nested dropdown">
							<span class="btn btn-primary">Administration</span>
							<div class="btn-group" role="group">
								<button id="btnGroupDrop1" type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"></button>
								<div class="dropdown-menu" aria-labelledby="btnGroupDrop1">
									<a class="dropdown-item" href="{{ path('app_register') }}">Enregistrer un administrateur</a>
									<a class="dropdown-item" href="{{ path('produit_index') }}">Produits</a>
									<a class="dropdown-item" href="{{ path('marque_index') }}">Marques</a>
									<a class="dropdown-item" href="{{ path('gamme_index') }}">Gammes</a>
									<a class="dropdown-item" href="{{ path('media_index') }}">Photos</a>

								</div>
							</div>
						</div>
					</li>
				{% endif %}
			</ul>
		</div>
	</nav>
</div>
